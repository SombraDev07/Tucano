# ambientes pixi
.pixi/*
!.pixi/config.toml
/pixi

# artefatos de build
*.mojoc
*.mojopkg

# canal conda gerado por `pixi run -e publicar empacotar` — vai para a branch
# gh-pages, nao para a main
/canal

# fixtures Parquet SAO commitadas (geradas por `pixi run -e fixtures fixtures`)

# saidas de teste
tests/fixtures/_saida_*.csv
tests/fixtures/_saida_*.parquet
tests/fixtures/_saida_*.arrow
tests/fixtures/_saida_*.xlsx
